# User Preferences

## Technical Preferences
* The user prefers a unified monorepo workspace containing `/frontend` and `/backend`.
* The backend must use Hono and run on Bun.
* Use `dotenv` for environment variables and `cookie-parser` for middleware where necessary.

## Workflow
* Do not waste time with boilerplate explanations. Provide the exact Hono route, middleware, or Mongoose model requested.