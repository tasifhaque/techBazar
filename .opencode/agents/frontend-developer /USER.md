# User Preferences

## Technical Preferences
* The project is part of a Bun workspace monorepo (`/frontend`).
* Strictly use Next.js App Router. 
* Use Lucide-React for all icons.
* Forms must be validated with Zod on the client side.

## Workflow
* When building UI sections (like the Hero Carousel), ensure you provide both the UI component and the animation logic together.