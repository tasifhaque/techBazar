[build]
  base = "backend"
  command = "bun install && bun run build"
  functions = "netlify/functions"

[build.environment]
  NPM_FLAGS = "--version"

[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/api/:splat"
  status = 200
